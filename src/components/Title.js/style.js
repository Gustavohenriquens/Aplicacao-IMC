import { StyleSheet } from 'react-native'

export default style = StyleSheet.create({
    boxTitle : {
        alignItems:'center',
        justifyContent:'center',
        padding : 10
    },
    textTitle : {
        color : '#FF0043',
        fontSize : 24,
        fontWeight : 'bold'
    }
})